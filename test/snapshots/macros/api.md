# API Reference

## Classes

| Name | Description |
|------|-------------|
| [`handlers`](handlers.md#handlers) | Registry of handler hooks. |

## Macros

{#max_retries}

### MAX_RETRIES

```cpp
#define MAX_RETRIES 5
```

The maximum number of retries.

{#clamp}

### CLAMP

```cpp
#define CLAMP(VALUE, LO, HI) ((VALUE) < (LO) ? (LO) : ((VALUE) > (HI) ? (HI) : (VALUE)))
```

Clamp a value into an inclusive range.

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `VALUE` |  | the value to clamp |
| `LO` |  | the lower bound |
| `HI` |  | the upper bound |

{#unreachable}

### UNREACHABLE

```cpp
#define UNREACHABLE() __builtin_unreachable()
```

Mark a code path as unreachable.

{#undocumented_flag}

### UNDOCUMENTED_FLAG

```cpp
#define UNDOCUMENTED_FLAG 1
```

{#retry_hook}

### RETRY_HOOK

```cpp
#define RETRY_HOOK registry->retry
```

Shorthand for the registry retry hook. Its initializer references another documented symbol, so Doxygen emits a cross-reference inside the initializer.

## Functions

{#reset}

### reset

```cpp
void reset(int)
```

Reset the retry counter.

{#configure}

### configure

```cpp
struct handlers * configure(struct handlers * target, int count)
```

Configure a handler registry.

#### Returns
the configured registry

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `target` | struct [`handlers`](handlers.md#handlers) * | the registry to configure |
| `count` | `int` | how many retries to allow |

## Variables

{#registry}

### registry

```cpp
struct handlers * registry
```

Generated by [Moxygen](https://0state.com/moxygen)